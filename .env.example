# Admin password for accessing the admin panel
ADMIN_PASSWORD=changeme123

# Port to run the server on
PORT=3000

# Optional: Public URL of this auth server (for CORS if needed)
# PUBLIC_URL=https://auth.yourdomain.com
