# Admin password for accessing the admin panel (REQUIRED - set a strong password)
ADMIN_PASSWORD=

# Port to run the server on
PORT=3000

# Node environment (development or production)
NODE_ENV=production

# Timezone for server logs (e.g., Europe/Vienna, UTC, America/New_York)
TZ=Europe/Vienna

# Optional: Public URL of this auth server (for CORS if needed)
# PUBLIC_URL=https://auth.yourdomain.com
